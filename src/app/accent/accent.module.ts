import { NgModule } from '@angular/core';
import { AccentComponent } from './accent.component';

@NgModule({
  declarations: [AccentComponent],
  exports: [AccentComponent]
})
export class AccentModule {
}
